@import 'variables.styl'

.profiles
  background-color: $primaryColor;
  font-family: $siteFont;
  padding-top: 100px;

  .profiles-info 
    background-color: #eee;
    padding: 40px;

    h2 
      font-weight: normal;
      font-size: 36px;
      margin-bottom: 10px;

    h3 
      font-weight: normal;
      font-size: 24px;
 
      

  